environment:
    global:
        PROJECT_NAME: hardback
    matrix:
        - TARGET: i686-pc-windows-gnu
        - TARGET: x86_64-pc-windows-msvc
        - TARGET: x86_64-pc-windows-gnu

install:
    - set PATH=C:\msys64\usr\bin;%PATH%
    - set PATH=C:\msys64\mingw32\bin;%PATH%
    - if "%TARGET%" == "x86_64-pc-windows-gnu" set PATH=C:\msys64\mingw64\bin;%PATH%
    - curl -sSf -o rustup-init.exe https://win.rustup.rs
    - rustup-init.exe --default-host %TARGET% --default-toolchain stable -y
    - set PATH=%PATH%;C:\Users\appveyor\.cargo\bin
    - rustc -Vv
    - cargo -V

build: false

test_script:
    - cmd: 'cargo build --release'

cache:
    - target -> Cargo.lock
    - C:\Users\appveyor\.cargo\registry -> Cargo.lock

before_deploy:
    - 7z a ../%PROJECT_NAME%-%TARGET%.zip *
    - appveyor PushArtifact ../%PROJECT_NAME%-%TARGET%.zip

deploy:
    provider: S3
    access_key_id: AKIAJYTGSC6HEOD2PLEQ
    secret_access_key:
      secure: Unrx4EIFXNJyoEG2osZLyyqTITJj6ea4XsHl/mSJ/WstEFBTZUQJSj6DDeCCykOy
    bucket: hardback
    region: ap-southeast-1
    artifact: /.*\.zip/
    encrypt: true